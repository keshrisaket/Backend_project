package com.example.ThreeProject.ThreeProject.repositery;

import com.example.ThreeProject.ThreeProject.modal.cloudModal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface cloudRepositery extends JpaRepository<cloudModal,Integer> {
}
